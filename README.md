# ПРИВЕТ!
## Описание
Это репозиторий для моего дипломного проекта.
Тема: «Разработка системы управления движением автономного электромеханического объекта с применением технического зрения» 
В данном случае я разрабатываю систему следования, а для опробования и экспериментов использую платформу Robomaster S1.
Для распознавания используется YOLOv11 (работает быстро на GPU), а для трекинга BOTSORT (имеет гибкие настройки).

## Железо, Python и OS
Запуск производился на ARDOR G15 с NVIDIA RTX 3050 и Intel i5-12450H.
OS Ubuntu Noble
python 3.8.20 (совместим с robomaster SDK в версиях 3.6.6 -> 3.8.9)

## Инструкции

### Установка

Для работы необходимо создать venv (виртуальное окружение) и активировать его:
```bash
python3.8 -m venv .venv
source .venv/bin/activate
```

Установить зависимости:
```bash
python3.8 -m pip install -r requirements.txt
```

> **Важно:** использовать именно `python3.8 -m pip`, а не просто `pip`. На Ubuntu `pip` и `python` могут указывать на системный Python (3.12).

### Запуск

```bash
source .venv/bin/activate
python3.8 main.py
```

### Работа с интерфейсом

1. **Подключить робота** — включить Robomaster S1, подключить компьютер к его Wi-Fi точке доступа, нажать кнопку **«Подключиться»** в интерфейсе.
2. **Запустить трекинг** — нажать **«Запуск трекинга»**. В левой части окна появится видеопоток с камеры робота.
3. **Выбрать цель** — кликнуть левой кнопкой мыши на нужного человека в видеообласти. Цель подсветится красным.
4. **Остановить** — кнопка **«Остановить»** прерывает трекинг и останавливает робота. **«Сброс цели»** сбрасывает выбранную цель без остановки камеры.

### Ручное управление (вне режима трекинга)

| Клавиша | Действие |
|---------|----------|
| W | Вперёд |
| S | Назад |
| A | Стрейф влево |
| D | Стрейф вправо |
| ESC | Выход |

> Ручное управление через клавиатуру доступно только когда трекинг **не запущен**.

## Версионирование

Пометки об истории изменений хранятся в VERSIONS.md
